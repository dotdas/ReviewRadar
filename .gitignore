.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.streamlit/secrets.toml

# Downloaded raw data and private user uploads must never be committed.
data/raw/
data/processed/
data/*.csv

# Model and evaluation artifacts are committed for reproducible deployment.
